export enum Events {
  EventText = 0,
  EventAction = 1,
  EventSeen = 2,
  EventFile = 3,
}

export enum EventType {
  IsTyping = "istyping",
  EndTyping = "endtyping",
  Joined = "joined",
  Waiting = "waiting",
  Offline = "offline",
}
